# ===== Required =====
# Get a key from https://serpapi.com/ (Google Jobs engine)
SERPAPI_KEY=replace-with-your-serpapi-key

# Email sending (example: Gmail SMTP over SSL). For Gmail, create an App Password.
SENDER_EMAIL=artistritesh@gmail.com
SENDER_NAME=Radiant Vibes Job Bot
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=artistritesh@gmail.com
SMTP_PASSWORD=replace-with-your-smtp-or-app-password

# Comma-separated list of recipients
RECIPIENT_EMAILS=artistritesh@gmail.com

# ===== Optional tuning =====
# Max results per query (pages). Each page returns up to ~10 results.
MAX_RESULTS_PER_QUERY=30

# Only include jobs posted within the last N days
DAYS_BACK_LIMIT=14

# If true, only keep jobs where keywords appear in title/description (stricter).
STRICT_MATCH=false
